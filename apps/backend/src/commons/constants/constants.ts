export enum EnvironmentEnum {
  Dev = 'dev',
  QA = 'qa',
  Staging = 'staging',
  Production = 'production',
}

export enum AuthProvider {
  Email = 'email',
  Google = 'google',
}

export enum DurationType {
  Day = 'day',
  Week = 'week',
  Month = 'month',
}