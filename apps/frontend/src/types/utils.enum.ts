export enum AuthProvider {
  Email = 'email',
  Google = 'google',
}

export enum RoleType {
  SystemAdmin = 'system_admin',
  SuperAdmin = 'super_admin',
  Admin = 'admin',
  Manager = 'manager',
  Designer = 'designer',
}
