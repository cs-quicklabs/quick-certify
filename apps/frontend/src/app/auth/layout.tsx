/**
 * Auth Callback Layout
 *
 * Simple layout for OAuth callback pages (no card wrapper needed)
 */
export default function AuthCallbackLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

